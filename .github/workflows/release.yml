name: Build

on:
  push:
    branches:
      - main

# 防止重叠 push 并发触发，导致重复打 Release / 抢提交
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 取 commit message，判断是否 build: 发版提交
      - name: Get commit message
        run: |
          COMMIT_MESSAGE=$(git log --format=%s -n 1)
          echo "commitmsg=${COMMIT_MESSAGE}" >> "$GITHUB_ENV"
      - name: Show commit message
        run: echo "$commitmsg"

      # 仅 build: 开头才发版；版本号取自 package.json 的 version 字段（不再从 commit message 解析）
      - name: Get version from package.json
        if: startsWith( env.commitmsg , 'build:' )
        run: |
          VERSION=$(jq -r .version package.json)
          echo "disttag=v${VERSION}" >> "$GITHUB_ENV"
          echo "disttag=v${VERSION}"

      # —— 以下仅发版提交执行 ——
      - name: Setup pnpm
        if: env.disttag
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        if: env.disttag
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        if: env.disttag
        run: pnpm install

      - name: Build
        if: env.disttag
        run: pnpm run build

      - name: Commit dist
        uses: EndBug/add-and-commit@v9
        if: env.disttag
        with:
          add: 'dist/index.user.js --force'
          default_author: github_actions
          message: 'commit ${{ env.disttag }} release asset'
          branch: main

      - name: Release
        uses: softprops/action-gh-release@v2
        if: env.disttag
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ env.disttag }}
          prerelease: false
          files: |
            dist/index.user.js
